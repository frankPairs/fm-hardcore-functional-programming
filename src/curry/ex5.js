// Bonus 2:
// ============
// use slice to define a function take()
// that takes n elements from an array. make it curried
const { slice } = require("./ex4");

const take = slice(0);

exports.take = take;
